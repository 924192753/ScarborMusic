# AGENTS.md

## Cursor Cloud specific instructions

This repository (`ScarborMusic`) is currently an empty scaffold with only a `README.md`. There are no source files, dependency manifests, build systems, or application code yet.

### Current state

- **No package manager or dependency files** (`package.json`, `requirements.txt`, `pyproject.toml`, etc.).
- **No build or run scripts** (`Makefile`, `docker-compose.yml`, CI config, etc.).
- **No application code** to lint, test, or run.

### For future agents

Once code is added, update this section with:
- How to install dependencies and which package manager to use.
- How to run the dev server, tests, and linter.
- Any non-obvious startup caveats or environment requirements.

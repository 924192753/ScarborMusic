# ScarborMusic 服务器部署（压缩包版）

> 完整步骤见：**docs/服务器部署教程.md**

## 快速开始

1. **前置**：CentOS Stream 9 + Docker + Compose，域名已解析，放行 80/443  
2. **上传**：将 `scarbormusic-deploy-*.tar.gz` 传到服务器 `/opt/`  
3. **解压**：

```bash
sudo mkdir -p /opt/scarbormusic
sudo tar -xzf /opt/scarbormusic-deploy-*.tar.gz -C /opt/scarbormusic
cd /opt/scarbormusic
```

4. **配置**：`cp .env.production.example .env.production` 并编辑密码与域名  
5. **部署**：`chmod +x scripts/*.sh && ./scripts/deploy.sh`  
6. **HTTPS**：`./scripts/init-ssl.sh`  
7. **检查**：`./scripts/health-check.sh`

详细说明、故障排查、运维命令 → **docs/服务器部署教程.md**

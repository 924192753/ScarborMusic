# ScarborMusic 服务器部署（公网 IP + HTTP）

> 完整步骤见：**docs/服务器部署教程.md**

| 项目 | 值 |
|------|-----|
| 公网 IP | **159.75.87.182** |
| 访问 | **http://159.75.87.182** |

## 快速开始

1. **前置**：CentOS Stream 9 + Docker，安全组放行 **80**、**22**  
2. **上传**：`scp scarbormusic-deploy-*.tar.gz root@159.75.87.182:/opt/`  
3. **解压**：

```bash
mkdir -p /opt/scarbormusic
tar -xzf /opt/scarbormusic-deploy-*.tar.gz -C /opt/scarbormusic
cd /opt/scarbormusic
```

4. **配置**：

```bash
cp .env.production.ip.example .env.production
vim .env.production   # 替换所有 change-me 密码
```

5. **部署**：`chmod +x scripts/*.sh && ./scripts/deploy-ip.sh`  
6. **访问**：http://159.75.87.182  

**不要**执行 `init-ssl.sh`（当前无 HTTPS）。

压缩包逐步教程 → **docs/压缩包部署教程.md**  
详细说明 → **docs/服务器部署教程.md**
